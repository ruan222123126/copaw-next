{
  "name": "nextai",
  "private": true,
  "version": "0.1.0",
  "description": "NextAI monorepo",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "contracts:check": "pnpm --dir tests/contract test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.23.0"
}
