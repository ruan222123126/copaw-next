{
  "name": "@nextai/tests-contract",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node --test *.test.mjs",
    "build": "node -e \"console.log('no build for contract tests')\"",
    "lint": "node openapi.lint.mjs"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  }
}
