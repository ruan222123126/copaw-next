{
  "name": "@nextai/tests-smoke",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node --test *.test.mjs",
    "build": "node -e \"console.log('no build for smoke tests')\"",
    "lint": "node -e \"console.log('no lint for smoke tests')\""
  }
}
